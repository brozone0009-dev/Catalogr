import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.catalogr.salesman',
  appName: 'Catalogr Salesman',
  webDir: 'dist'
};

export default config;
